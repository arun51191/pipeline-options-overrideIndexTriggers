pipeline {	
    agent any	
    options {	
		timestamps()
		overrideIndexTriggers(true)
    }
    stages {	
        stage('Build') {	
            steps {
                echo 'Hello World 3...now jenkins wont build the job automatically even commit is made'
            }	
        }	
    }	
}	
